// <auto-generated> Rust port of template.test.generated.cs for `Principal`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (PrincipalLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct PrincipalTest;

impl PrincipalTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        let v = BaseTest::get_test_data(&record, "VARCHAR", "firstnames", "50");
        record.set("first_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "lastnames", "50");
        record.set("last_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "os_user", "50");
        record.set("username", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "emailAddresses", "100");
        record.set("email", v);
        
        record.set("principal_status_id", serde_json::json!(BaseTest::get_last_id("principalstatus")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("created_date", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("last_login_date", v);
                Logger::info(format!("Testing PrincipalLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        PrincipalLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("principal", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = PrincipalLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"first_name", "last_name", "username", "email", "principal_status_id",         ];
        BaseTest::assert_persisted("Principal::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("principal");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = PrincipalLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        let v = BaseTest::get_test_data(&record, "VARCHAR", "firstnames", "50");
        record.set("first_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "lastnames", "50");
        record.set("last_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "os_user", "50");
        record.set("username", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "emailAddresses", "100");
        record.set("email", v);
        
        record.set("principal_status_id", serde_json::json!(BaseTest::get_last_id("principalstatus")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("created_date", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("last_login_date", v);
                Logger::info(format!("Testing PrincipalLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        PrincipalLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = PrincipalLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"first_name", "last_name", "username", "email", "principal_status_id",         ];
        BaseTest::assert_persisted("Principal::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
